{
      "rules": {
          ".read": false,
              ".write": false,
                  "users": {
                        "$uid": {
                                ".read": "auth != null && auth.uid == $uid",
                                        ".write": "auth != null && auth.uid == $uid && (!data.exists() ? newData.hasChildren(['role']) && newData.child('role').val() == 'admin' : newData.child('role').val() == data.child('role').val() && ((!data.child('empresaId').exists() && newData.child('empresaId').exists() && root.child('empresas').child(newData.child('empresaId').val()).child('info').child('criadoPor').val() == auth.uid) || (data.child('empresaId').exists() && newData.child('empresaId').val() == data.child('empresaId').val())))"
                                              }
                                                  },
                                                      "empresas": {
                                                            "$empresaId": {
                                                                    "info": {
                                                                              ".read": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId",
                                                                                        ".write": "auth != null && (root.child('users').child(auth.uid).child('empresaId').val() == $empresaId || (!data.exists() && newData.child('criadoPor').val() == auth.uid))",
                                                                                                  ".validate": "newData.hasChildren(['nome', 'criadoPor']) && newData.child('criadoPor').isString()"
                                                                                                          },
                                                                                                                  "$collection": {
                                                                                                                            ".read": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId",
                                                                                                                                      ".write": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId",
                                                                                                                                                ".indexOn": ["data", "status", "codigo", "cliId", "clienteId", "fornecedorId"]
                                                                                                                                                        }
                                                                                                                                                              }
                                                                                                                                                                  }
                                                                                                                                                                    }
                                                                                                                                                                    }
                                                                                                                                                                    
}